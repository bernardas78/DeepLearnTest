import numpy as np

def backProp(params, za, y, debug=False):
    #   params
    #   za
    #   y
    #Returns: grads (dictionary) which contains keys:
    #           dW1, .., dWL - where L number of layers
    #           db1, .., dbL
    return 1
